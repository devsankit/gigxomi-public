Place a licensed chat notification audio file at:

```
public/sounds/chat-notification.mp3
```

The chat UI will use this file for foreground/in-page message alerts. If the file is not present or the browser blocks playback, it falls back to the built-in generated chime.
